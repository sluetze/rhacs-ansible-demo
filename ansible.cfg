[defaults]
# ansible.eda still registers deprecated aliases (activation → rulebook_activation) on load.
deprecation_warnings = False
collections_paths = ./collections:~/.ansible/collections:/usr/share/ansible/collections
